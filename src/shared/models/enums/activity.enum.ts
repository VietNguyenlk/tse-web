export enum ActivityType {
  CONTEST,
  SEMINAR,
  TRAINING,
}

export enum ActivityStatus {
  PLANED,
  OPENED,
  CLOSED,
  CANCELED,
}

export enum ActivityScope {
  INTERNAL,
  EXTERNAL,
}

export enum VenueType {
  OFFLINE,
  ONLINE,
}
